import {
    loadOrGenerateKeypair,
  } from "@/tiplink/utils/helpers";

// generate a new keypair for use in this demo
 //const payer = loadOrGenerateKeypair("payer");
 //log(`Send 0.05 SOL to this wallet ${payer.publicKey}`)
